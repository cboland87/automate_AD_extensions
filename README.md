# automate_AD_extensions
Production IAM automation with regex-validated input, regex-parsed AD attribute extraction, atomic two-attribute update (telephoneNumber + extensionAttribute11), timestamped audit logging, and dry-run confirmation prompt.
